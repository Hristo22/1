function despedir(){
    var despedida="Adios";
    alert(despedida);
}
// Invocar funcion
despedir();

// Muestra la variable desde fuera de la función mediante un window.alert
window.alert(despedir());